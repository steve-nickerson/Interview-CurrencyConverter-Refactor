import {assert} from 'chai';

describe('App', function () {
    describe('GET /api/currencyConverter', function () {
        it('should return 200 OK', function () {
            // TODO: Write a test that verifies the response status code is 200.
            assert(true);
        });

        it('should return the converted amount', function () {
            // TODO: Write a test that verifies the response body is the converted amount.
            assert(true);
        });
    });
});